'use client';

import type { ReactNode } from 'react';

// Esta es una versión temporal y neutralizada de AppShell para arreglar la compilación.
// No contiene ninguna lógica, solo muestra el contenido que se le pasa.
export function AppShell({ children }: { children: ReactNode }) {
    return <>{children}</>;
}
